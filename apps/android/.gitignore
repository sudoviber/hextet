# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# Android SDK machine-local config (never commit a machine path)
local.properties

# IDE
.idea/
*.iml
.DS_Store

# Build outputs
app/build/
captures/

# Rust build artifacts copied into the app (see README: they are produced by
# `cargo build --target aarch64-linux-android`, not checked in).
app/src/main/jniLibs/**/*.so
