# hextet M7（Android）实现计划

> 本计划覆盖 spec §8 M7 行的交付：engine FFI 化（UniFFI）、VpnService 前台服务、
> gotatun 数据面（Mullvad Android 生产同款）、按需连接模式。v1.0 必含。

**设计依据:** `docs/superpowers/specs/2026-08-06-hextet-design.md` §8 M7 / §9 / §10、
ADR-0012（gotatun 数据面已迁移）、`docs/dev/gotatun-migration.md`（数据面 API 地图）。

**前置已就绪（2026-08-14）：**
- gotatun 0.8.1 已作为跨平台数据面落地（`crates/wg-userspace`，ADR-0012）。
- `hextet_engine::daemon` 已支持 `DaemonHandle::spawn` + `shutdown`（进程内运行 + 优雅
  停机），是 Android 在进程内跑 daemon 的关键前置。

---

## 1. 关键架构差异（Android vs 桌面）

- **TUN 设备不是 `tun` crate 开的**：Android 的 TUN fd 由 `VpnService.Builder.establish()`
  返回（Android 系统分配的 fd），不是 `TunDevice::from_name`。gotatun 的 `DeviceBuilder`
  已支持 `with_ip`/`with_ip_pair` 注入自定义 `IpSend`/`IpRecv`——把 VpnService fd 包一层
  `IpSend`/`IpRecv` 适配器即可（这是 gotatun `tun` feature 之外的 Android 专用 transport）。
- **进程内运行**：Android App 就是进程，daemon 经 `DaemonHandle::spawn` 跑在 App 的
  tokio runtime 里，无跨进程状态问题（state.json 在 App 私有目录）。
- **按需连接**：spec 要求「打洞 <1s、无常驻 keepalive 省电」——这是 VpnService 的
  `on-demand` 模式 + 快速握手，属后续优化切片，不在第一片。

## 2. 迁移切片（按独立性 × 可验证性排序）

| 切片 | 状态 | 交付 |
|---|---|---|
| **A engine FFI（UniFFI）** | ✅ 已做（2026-08-14） | 独立 crate `crates/engine-ffi`（UniFFI 0.32）：UDL + build.rs `generate_scaffolding` + `include_scaffolding!` 导出 `load_config` / `status` / `daemon_spawn` / `daemon_shutdown`（返回 JSON 串，错误约定 `{"error":...}`）。注意：实际用的是 **UDL**（不是 §3 初稿的 `#[uniffi::export]` proc-macro），且 `status` 走 state.json v7（含 WG 统计，跨线程/跨进程读），见 CHANGELOG「hextet-engine-ffi」 |
| **B VpnService 壳** | ⬜ 待做 | `apps/android/` Gradle 项目（Kotlin）：VpnService `establish()` 拿 fd → 包成 gotatun transport → 经 FFI `daemon_spawn` 进程内跑 daemon |
| **C gotatun in-process 数据面** | ⬜ 待做 | `wg-userspace` 的 Android transport（VpnService fd 适配 `IpSend`/`IpRecv`）；`apply` 走 `with_ip`（不经 `TunDevice::from_name`） |
| **D 按需连接 + 省电** | ⬜ 待做 | `on-demand` 模式、握手加速、keepalive 分级（spec §5 移动端省电） |

## 3. 切片 A 的关键点（本计划先行）

- **FFI 工具**：UniFFI（spec §8 定）。用 `#[uniffi::export]` proc-macro（不用 UDL）定义
  接口；`uniffi-bindgen` 生成 Kotlin 绑定。Rust 侧 scaffolding 可在 macOS 编译 + 单测。
- **FFI 表面**（最小可用集）：
  - `load_config(path: String) -> Result<String, String>`（返回 JSON 或错误串）。
  - `daemon_spawn(path: String) -> Result<u64, String>`（句柄 id，内部存 DaemonHandle）。
  - `daemon_shutdown(handle: u64) -> Result<(), String>`。
  - `status(path: String) -> Result<String, String>`（`build_report` → JSON）。
  - `daemon_status(handle: u64) -> Result<String, String>`（从运行中的 daemon 读 state.json，
    含 WG 统计——需 daemon 把 WG 统计写进 state.json，见下）。
- **跨线程 status 的缺口**：`build_report` 里的 WG 统计（rx/tx/last_handshake）来自
  `backend.status()`（进程内 gotatun Device）。FFI 的 `daemon_status` 与 daemon 同一进程但
  不同线程，仍需一个「WG 统计进 state.json」的补充切片（与 CLI `hextet status` 的跨进程
  读取同一缺口，一并解决）。**第一片可先只返回 state.json 已有的打洞状态，WG 统计标注
  TODO**，不阻塞 FFI 落地。

## 4. 诚实边界

- **Android 需要 SDK/NDK + Gradle + 真机/模拟器**：本机是 macOS、无 Android SDK，
  切片 B/C/D 无法本机运行时验证；切片 A 的 Rust scaffolding 可本机编译 + 单测，Kotlin
  绑定生成后仅编译检查（无 Android 项目可编译）。
- **VpnService 单一槽位**：与代理类 App 冲突（spec §2 诚实边界），文档需给出指引。
- **gotatun 是 pre-1.0**：锁死 `=0.8.1` + `cargo deny` 常开（ADR-0012）。

## 5. 验收（切片 A）

- `cargo test`/`clippy` 本机全绿；UniFFI scaffolding 编译通过；`status`/`daemon_spawn`/
  `shutdown` 的 Rust 单测覆盖（无 root、无真实数据面——`status` 读临时 state.json，
  `daemon_spawn` 用 `MockBackend` 或空后端路径）。
